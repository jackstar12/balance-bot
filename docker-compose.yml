version: '3.9'
services:
  init:
    depends_on:
      - pg-server
    build:
      dockerfile: init.Dockerfile
      context: .
  migrate:
    depends_on:
      - pg-server
    build:
      dockerfile: migrations.Dockerfile
      context: .
  api:
    depends_on:
      - migrate
    build:
      dockerfile: tradealpha/api/Dockerfile
      context: .
    ports:
      - "5000:5000"
  collector:
    depends_on:
      - migrate
    build:
      dockerfile: tradealpha/collector/Dockerfile
      context: .
  bot:
    depends_on:
      - migrate
    build:
      dockerfile: tradealpha/bot/Dockerfile
      context: .
  redis:
    image: "redis:latest"
    container_name: redis-server
    ports:
      - "6379:6379"
  pg-server:
    image: "postgres"
    container_name: pg-server
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data
    environment:
      - "POSTGRES_PASSWORD=postgres"
      - "POSTGRES_USER=postgres"
      - "POSTGRES_DB=postgres"

volumes:
  pg-data:
